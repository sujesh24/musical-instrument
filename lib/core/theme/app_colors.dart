import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const Color scaffoldBackground = Color(0xFFF6E6D1);

  // Cards
  static const Color primaryCard = Color(0xFF27201A);
  static const Color secondaryCard = Color(0xFF4D3421);

  // Brand / Accent
  static const Color primary = Color(0xFFD7A24B);

  // Borders & Dividers
  static const Color border = Color(0xFFC9B18D);

  // Text
  static const Color primaryText = Color(0xFF3A2A1E);
  static const Color onDarkText = Colors.white;
  static const Color secondaryText = Color(0xFFC9B18D);

  // Status
  static const Color success = Color(0xFF6AC86C);
}
